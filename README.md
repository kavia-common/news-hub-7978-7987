# news-hub-7978-7987

This workspace contains the News Hub React frontend integrating with the GNews API. It supports top headlines, categories, search, bookmarking, and sharing with no setup required.

Default API key: The app now uses a provided GNews API key by default for zero‑configuration:
- Default key used at runtime: 25e269348ded27d9bf00f28bfca0445a
- You can override this using the environment variable REACT_APP_GNEWS_API_KEY in a `.env` file if desired.